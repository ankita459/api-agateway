server:
  port: 8093

spring:
  application:
    name: microservice3
  data:
    mongodb:
      uri:  mongodb://localhost:27017/test

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone : http://localhost:8761/eureka/
  instance:
      hostname: localhost  

    